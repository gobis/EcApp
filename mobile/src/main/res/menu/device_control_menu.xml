<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:id="@+id/grp1" >
        <!-- Search / will display always -->
        <item
            android:id="@+id/action_settings"
            android:title="@string/settings"
            app:showAsAction="never" />

        <!-- Search / will display always -->

        <item
            android:id="@+id/action_help"
            android:title="@string/help"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_scan_device"
            android:title="@string/scan_device"
            app:showAsAction="never" />

        <item
            android:id="@+id/action_clear_log"
            android:title="@string/clear_log"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_enable_log"
            android:title="@string/enable_log"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_disable_log"
            android:title="@string/enable_log"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_send_text"
            android:title="@string/text_command"
            app:showAsAction="never" />
    </group>

</menu>